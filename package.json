{
  "name": "jit-debug-monorepo",
  "private": true,
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "start": "pnpm -r start",
    "prisma:generate": "pnpm -r prisma:generate",
    "prisma:migrate": "pnpm -r prisma:migrate",
    "seed": "pnpm --filter @jit-debug/api seed"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
